<?php

/*
 * t_site(SiteID, Domain, UpVotes, DownVotes)
 * t_user(UserID, BrowserAgent)
 * t_site_user(SiteUserID, SiteID, UserID, Vote)
 *
 */

if(isset($_GET['domain']))
{
    $domain = $_GET['domain'];

    $db = mysqli_connect("lighning.servegame.com", "root", "P@ssword", "rankit" );

    $query = "INSERT INTO t_site (Domain) VALUE ('".$domain."')";

    mysqli_query($db,$query);

    if($db->affected_rows > 0)
    {
        echo "success";
    }

}